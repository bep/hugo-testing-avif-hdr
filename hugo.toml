baseURL = "https://example.com/"
title   = "HDR vs SDR"

# Only home for now, remove what you want.
disableKinds = [
    "taxonomy",
    "term",
    "404",
    "page",
    "robotstxt",
    "rss",
    "sitemap",
]

[imaging.avif]
    encoderspeed = 9

[module]
    [[module.mounts]]
        source = 'content/guest'
        target = 'content'
